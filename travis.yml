#define the language and version we are using
language: python
python:
  - "3.7"
# define the code dependencies for the code
install:
  - pip install -r requirements.txt
# define and run some tests on our code
script:
  - pytest tests.py
